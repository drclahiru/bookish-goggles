let id = (x) {
    x
}

let apply = (f, x) {
    f(x)
}

let a Number = 2

let square = (x) {
    x * x
}

let b = apply(square, id)

let c = apply(id, square)


let f = (x) {
    a + x
}

let main = f(4)